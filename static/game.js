(() => {
  const CFG=window.GAME_CONFIG, USER=Number(CFG.user_id), $=id=>document.getElementById(id);
  const boardEl=$('gameBoard'), statusEl=$('gameStatus'), historyEl=$('moveHistory'), statusBar=$('statusBar'), resignBtn=$('resignBtn'), rematchBtn=$('rematchBtn');
  const gameChatMessages=$('gameChatMessages'), gameChatForm=$('gameChatForm'), gameChatInput=$('gameChatInput'), gameChatSend=$('gameChatSend');
  let current=null, selectedChecker=null, loading=false, sending=false, lastSignature='';
  let gameChatLastId=0, loadingGameChat=false, sendingGameChat=false, gameChatInitialized=false;
  function escapeHtml(v){const d=document.createElement('div');d.textContent=v??'';return d.innerHTML;}
  function notify(msg,error=false){statusBar.textContent=msg;statusBar.classList.remove('hidden','error');if(error)statusBar.classList.add('error');setTimeout(()=>statusBar.classList.add('hidden'),4500);}
  function setTurnBanner(g){
    if(g.status==='finished'){
      const text=g.winner_user_id ? (g.winner_user_id===USER?'Você venceu a partida!':`${g.winner_name} venceu a partida.`) : 'Partida encerrada.';
      statusEl.textContent=text;statusEl.className='game-turn-banner finished';resignBtn.classList.add('hidden');rematchBtn.classList.remove('hidden');return;
    }
    const mine=g.is_my_turn; statusEl.textContent=mine?'Sua vez de jogar':`Aguardando ${g.opponent_name} jogar`; statusEl.className=`game-turn-banner ${mine?'mine':'waiting'}`; resignBtn.classList.remove('hidden');rematchBtn.classList.add('hidden');
  }
  async function sendMove(payload){if(sending)return;sending=true;try{const r=await fetch(`/api/games/${CFG.code}/move`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const data=await r.json();if(!r.ok)throw new Error(data.error||'Jogada recusada.');current=data.game;selectedChecker=null;render(current);}catch(e){notify(e.message,true);}finally{sending=false;}}
  function renderTic(g){const s=g.state,b=s.board||[];const wrap=document.createElement('div');wrap.className='ttt-board';b.forEach((cell,i)=>{const btn=document.createElement('button');btn.type='button';btn.className=`ttt-cell ${cell?'filled':''}`;btn.textContent=cell||'';btn.disabled=!g.is_my_turn||g.status!=='active'||!!cell;btn.addEventListener('click',()=>sendMove({index:i}));wrap.appendChild(btn);});boardEl.replaceChildren(wrap);}
  function checkerPiece(cell){if(!cell)return '';const side=cell.toLowerCase();const king=cell===cell.toUpperCase();return `<span class="checker-piece ${side==='a'?'side-a':'side-b'} ${king?'king':''}">${king?'★':''}</span>`;}
  function renderCheckers(g){const s=g.state,board=s.board||[];const mySide=(s.side_by_user||{})[String(USER)];const reverse=mySide==='a';const wrap=document.createElement('div');wrap.className='checkers-board';const rows=[0,1,2,3,4,5,6,7],cols=[0,1,2,3,4,5,6,7];if(reverse){rows.reverse();cols.reverse();}
    for(const r of rows)for(const c of cols){const btn=document.createElement('button');btn.type='button';btn.className=`checker-cell ${(r+c)%2?'dark':'light'}`;btn.dataset.r=r;btn.dataset.c=c;const cell=board[r]?.[c];btn.innerHTML=checkerPiece(cell);if(selectedChecker&&selectedChecker[0]===r&&selectedChecker[1]===c)btn.classList.add('selected');if(s.forced_from&&Number(s.forced_from[0])===r&&Number(s.forced_from[1])===c)btn.classList.add('forced');btn.disabled=!g.is_my_turn||g.status!=='active';btn.addEventListener('click',()=>{if(!g.is_my_turn)return;const piece=board[r]?.[c];if(piece&&piece.toLowerCase()===mySide){selectedChecker=[r,c];renderCheckers(g);return;}if(selectedChecker){sendMove({from:selectedChecker,to:[r,c]});}});wrap.appendChild(btn);} boardEl.replaceChildren(wrap);
    const note=document.createElement('div');note.className='game-help';note.textContent=s.forced_from?'Você capturou uma peça: continue com a peça destacada.':'Capturas são obrigatórias quando disponíveis. As damas coroadas movem uma casa diagonal em qualquer direção.';boardEl.appendChild(note);
  }
  function cardHtml(card, playable=false){const red=['♦','♥'].includes(card.suit);return `<button class="playing-card ${red?'red':''} ${playable?'':'disabled'}" ${playable?'':'disabled'} data-card="${escapeHtml(card.id)}" type="button"><span class="card-rank">${escapeHtml(card.rank)}</span><span class="card-suit">${escapeHtml(card.suit)}</span></button>`;}
  function renderTruco(g){const s=g.state;const wrap=document.createElement('div');wrap.className='truco-table';const score=s.score||{};wrap.innerHTML=`<div class="truco-score"><div><span>Você</span><strong>${Number(score[String(USER)]||0)}</strong></div><div><span>${escapeHtml(g.opponent_name)}</span><strong>${Number(score[String(g.opponent_id)]||0)}</strong></div></div><div class="truco-info"><span>Mão ${Number(s.hand_no||1)}</span><span>Vira: <b>${escapeHtml(s.vira?.rank||'')}${escapeHtml(s.vira?.suit||'')}</b></span><span>Manilha: <b>${escapeHtml(s.manilha_rank||'')}</b></span></div><div class="opponent-hand"><small>Cartas de ${escapeHtml(g.opponent_name)}</small><div class="card-row">${'<span class="card-back">◆</span>'.repeat(Number(s.opponent_card_count||0))}</div></div><div class="truco-played"><small>Mesa</small><div class="played-row">${(s.played||[]).slice(-2).map(p=>`<div class="played-card"><span>${p.user_id===USER?'Você':escapeHtml(g.opponent_name)}</span>${cardHtml(p.card,false)}</div>`).join('')||'<span class="muted">Nenhuma carta nesta rodada.</span>'}</div></div><div class="my-hand"><small>Suas cartas</small><div class="card-row" id="myCards">${(s.my_hand||[]).map(c=>cardHtml(c,g.is_my_turn&&g.status==='active')).join('')}</div></div>${s.last_hand_result?`<div class="game-help">${escapeHtml(s.last_hand_result)}</div>`:''}<div class="game-help">Modo básico: 3 cartas por mão, vira/manilha e placar até 12. A opção de pedir Truco pode ser adicionada depois.</div>`;boardEl.replaceChildren(wrap);wrap.querySelectorAll('.playing-card:not(.disabled)').forEach(btn=>btn.addEventListener('click',()=>sendMove({card_id:btn.dataset.card})));}
  function renderHistory(g){const moves=g.recent_moves||[];historyEl.innerHTML='';if(!moves.length){historyEl.innerHTML='<div class="empty-state">A partida ainda não tem jogadas.</div>';return;}for(const m of moves.slice().reverse()){const row=document.createElement('div');row.className='game-move-row';row.innerHTML=`<strong>${escapeHtml(m.player_name)}</strong><span>${escapeHtml(m.summary)}</span><small>${new Date(m.created_at).toLocaleString('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})}</small>`;historyEl.appendChild(row);}}
  function gameChatTime(value){try{return new Date(value).toLocaleString('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'});}catch(_){return '';}}
  function appendGameChatMessage(m,scroll=true){
    if(!gameChatMessages||gameChatMessages.querySelector(`[data-chat-id="${Number(m.id)}"]`))return;
    gameChatMessages.querySelector('.game-chat-empty')?.remove();
    const row=document.createElement('div');row.className=`game-chat-row ${m.mine?'mine':'other'}`;row.dataset.chatId=String(m.id);
    const avatar=m.author_photo_url?`<img src="${escapeHtml(m.author_photo_url)}" alt="">`:`<span>${escapeHtml((m.author_name||'?').slice(0,1).toUpperCase())}</span>`;
    row.innerHTML=`<div class="game-chat-avatar">${avatar}</div><div class="game-chat-bubble"><div class="game-chat-meta"><strong>${m.mine?'Você':escapeHtml(m.author_name)}</strong><time>${gameChatTime(m.created_at)}</time></div><div class="game-chat-text">${escapeHtml(m.text)}</div></div>`;
    gameChatMessages.appendChild(row);gameChatLastId=Math.max(gameChatLastId,Number(m.id)||0);if(scroll)gameChatMessages.scrollTop=gameChatMessages.scrollHeight;
  }
  async function loadGameChat(){if(!gameChatMessages||loadingGameChat)return;loadingGameChat=true;try{const r=await fetch(`/api/games/${CFG.code}/chat?after=${gameChatLastId}`,{cache:'no-store'});if(r.status===401){location.href='/login';return;}const data=await r.json();if(!r.ok)throw new Error(data.error||'Não foi possível carregar o chat.');const messages=data.messages||[];if(!gameChatInitialized){gameChatMessages.innerHTML='';gameChatInitialized=true;}for(const m of messages)appendGameChatMessage(m,true);if(!gameChatMessages.children.length)gameChatMessages.innerHTML='<div class="empty-state game-chat-empty">Nenhuma mensagem nesta partida ainda.</div>';}catch(e){console.warn(e);}finally{loadingGameChat=false;}}
  async function sendGameChat(){if(sendingGameChat||!gameChatInput)return;const text=gameChatInput.value.trim();if(!text)return;sendingGameChat=true;gameChatSend.disabled=true;try{const r=await fetch(`/api/games/${CFG.code}/chat`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Não foi possível enviar.');const empty=gameChatMessages?.querySelector('.game-chat-empty');if(empty)empty.remove();appendGameChatMessage(data.message,true);gameChatInput.value='';gameChatInput.focus();}catch(e){notify(e.message,true);}finally{sendingGameChat=false;gameChatSend.disabled=false;}}
  function render(g){setTurnBanner(g);if(g.game_type==='tictactoe')renderTic(g);else if(g.game_type==='checkers')renderCheckers(g);else if(g.game_type==='truco')renderTruco(g);renderHistory(g);}
  async function load(){if(loading||sending)return;loading=true;try{const r=await fetch(`/api/games/${CFG.code}`,{cache:'no-store'});if(r.status===401){location.href='/login';return;}const data=await r.json();if(!r.ok)throw new Error(data.error||'Partida não encontrada.');const sig=JSON.stringify([data.updated_at,data.turn_user_id,data.status,data.recent_moves?.length]);current=data;if(sig!==lastSignature){lastSignature=sig;render(data);}}catch(e){notify(e.message,true);}finally{loading=false;}}
  resignBtn.addEventListener('click',async()=>{if(!confirm('Desistir desta partida? A outra pessoa será declarada vencedora.'))return;const r=await fetch(`/api/games/${CFG.code}/resign`,{method:'POST'});const data=await r.json();if(r.ok){current=data.game;render(current);}else notify(data.error||'Não foi possível desistir.',true);});
  rematchBtn.addEventListener('click',async()=>{rematchBtn.disabled=true;try{const r=await fetch(`/api/games/${CFG.code}/rematch`,{method:'POST'});const data=await r.json();if(!r.ok)throw new Error(data.error||'Não foi possível criar revanche.');location.href=data.url;}catch(e){notify(e.message,true);rematchBtn.disabled=false;}});
  gameChatForm?.addEventListener('submit',e=>{e.preventDefault();sendGameChat();});
  gameChatInput?.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendGameChat();}});
  load();loadGameChat();setInterval(load,2200);setInterval(loadGameChat,1200);setInterval(()=>NossaSala.ping(),20000);
})();
